import 'package:flutter/material.dart';
import 'package:myapp/pages/tapbox.dart';

class StatusScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: TapboxA(),
    );
  }
}